from tkinter import *
from os import system
from tkinter import filedialog
from PIL import Image, ImageTk
from Interface import GuInterface
#
# def image_select():
#     filename = filedialog.askopenfilename()
#     # command =


window = Tk()
interface = GuInterface(window)


# window.title('Image Watermarking Desktop App')
# window.config(padx=100, pady=100, bg='black')

# canvas



# # labels & buttons
# title_label = Label(text='Watermarking App', font=('Arial', 50, 'bold'), bg='yellow')
# # title_label.config()
# title_label.grid(column=1, row=0)
#
# image_button = Button(text='Select An Image', font=('Arial', 10, 'normal'))
# # image_button.config()
# image_button.grid(column=0, row=3)
#
# upload_watermark = Button(text='Upload Watermark', font=('Arial', 10, 'normal'))
# # upload_watermark.config()
# upload_watermark.grid(column=1, row=2)
#
# apply_watermark = Button(text='Apply Watermark', font=('Arial', 10, 'normal'))
# # apply_watermark.config()
# apply_watermark.grid(column=2, row=3)


window.mainloop()
